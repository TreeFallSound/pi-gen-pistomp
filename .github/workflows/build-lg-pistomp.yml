name: build-lg-pistomp

on:
  push:
    branches: [main]
    paths:
      - 'debpkgs/lg-pistomp/**'
      - 'config.sh'
  pull_request:
    paths:
      - 'debpkgs/lg-pistomp/**'
      - 'config.sh'
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/build-deb.yml
    with:
      pkg: lg-pistomp
    secrets: inherit
