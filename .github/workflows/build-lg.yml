name: build-lg

on:
  push:
    branches: [main]
    paths:
      - 'debpkgs/lg/**'
      - 'config.sh'
  pull_request:
    paths:
      - 'debpkgs/lg/**'
      - 'config.sh'
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/build-deb.yml
    with:
      pkg: lg
    secrets: inherit
