name: build-loopjefe-lv2

on:
  push:
    branches: [main]
    paths:
      - 'debpkgs/loopjefe-lv2/**'
  pull_request:
    paths:
      - 'debpkgs/loopjefe-lv2/**'
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/build-deb.yml
    with:
      pkg: loopjefe-lv2
    secrets: inherit
