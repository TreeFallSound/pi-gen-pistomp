# Allow the pistomp user passwordless sudo (matches pistomp-arch's wheel group setup).
# Required by pistomp-recovery (OTA upgrades) and modify_version.sh (sudo sed on /etc/jackdrc).
pistomp ALL=(ALL) NOPASSWD: ALL
