# Kernel debs are gitignored

You must run `./build-rt-kernel-docker.sh` to download the linux sources and build the current kernel, if you do not already have it locally.

The script will deposit packages for (headers, image, libc) in this directory.
